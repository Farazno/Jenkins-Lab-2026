pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Jenkinsfile detected successfully!'
            }
        }
        stage('Build') {
            steps {
                echo 'Building... (This is where your build commands go)'
            }
        }
    }
}
